{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "name": "Rexa app",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
        }
    ]
}